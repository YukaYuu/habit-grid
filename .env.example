# Postgres connection string
DATABASE_URL=postgresql://habit_grid:habit_grid@localhost:5433/habit_grid

# Shared password for the single login gate (see lib/auth.ts)
APP_PASSWORD=

# Random string used to sign the session cookie; generate with `openssl rand -hex 32`
SESSION_SECRET=
